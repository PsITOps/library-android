<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ps.pslibrary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.LibraryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="pslibrary.registeractivity.RegisterActivity" />
        <activity android:name="pslibrary.booksactivity.BooksActivity" />
        <activity android:name="pslibrary.mybooksactivity.MyBooksActivity" />
        <activity android:name="pslibrary.bookinformationactivity.BookInformationActivity" />
        <activity android:name="pslibrary.addbookactivity.AddBookActivity" />


    </application>

</manifest>